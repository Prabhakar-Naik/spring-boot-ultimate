# it will hold the sensitive static data on functionality
#prefer in test environment not in production usage.
#

spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:spirit;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.username=prabhakar
spring.datasource.password=pass